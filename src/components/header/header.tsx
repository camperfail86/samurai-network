import style from './header.module.css'
import logo from '../../img/logo.jpg';

export const Header = () => {
  return (
    <header className={style.header}>
      <img src={''} alt="Логотип." />
    </header>
  )
}
